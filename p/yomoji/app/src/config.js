// config.js — 全チューニング定数の一元管理
//
// 数値の根拠は docs/game-design.md §2(基本ルール)・§3(文字セット)・
// adr/0003(判定・回数の設計理由)を参照。姉妹作と同様、ここを直せば
// 全体の難易度・体感が変わる想定。

export function defaultConfig() {
  return {
    // 答えの文字数(ひらがな。小書き・濁点も1文字と数える。例: きょうと=4文字)
    length: 4,

    // 挑戦回数。仮名74種(英26字の約2.8倍)+「おしい」の情報量を踏まえた値。
    // 体感調整でここだけ変えればよい(adr/0003 §2)
    tries: 8,
  };
}
