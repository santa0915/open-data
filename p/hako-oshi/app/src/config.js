// config.js — 全チューニング定数の一元管理
//
// 手触り・ソルバの探索上限に関わる数値はすべてここに集約する。各値の意味と
// 「変えると何が起きるか」は docs/development-guide.md §3 のチューニング表を参照。
// 本作は完全決定的(乱数レス)のため rng 関連の設定は存在しない。

export function defaultConfig() {
  return {
    solver: {
      // 状態数上限。ヒントの応答時間と検証可能性のトレードオフ(adr/0003)。
      // 内寸9×8・箱4個の設計バジェットなら実レベルは数千〜数万状態で収まる。
      maxStates: 200000,
    },

    input: {
      repeatMs: 150, // 押しっぱなし移動を連続入力とみなす間隔 [ms]
      swipeThreshold: 24, // スワイプと判定する最小移動量 [px](小さいと誤反応、大きいと鈍い)
    },

    animation: {
      moveDurationMs: 120, // 1マス移動のCSSトランジション秒数(見た目のみ。入力はキューしない)
    },
  };
}
